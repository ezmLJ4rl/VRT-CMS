# Leave unset during local development — the Vite dev server proxies /api to
# http://localhost:4000 automatically (see vite.config.js).
#
# For a production build, set this to your deployed API's base URL:
# VITE_API_URL=https://api.yourchurch.org/api
