// What a table cell shows when the record has no value there.
//
// An en dash is the conventional "no value" glyph: it reads as a deliberate
// blank, and unlike a comma or a colon it cannot be mistaken for punctuation
// belonging to the record itself. One constant, so every screen shows the same
// silences the same way.
export const EMPTY_VALUE = '–';
